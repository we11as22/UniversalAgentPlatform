# Triton Model Repository

This repository contains versioned model directories for self-hosted inference. Every self-hosted capability must be exposed through Triton and consumed through `provider-gateway`.

Capabilities:

- `llm/`
- `asr/`
- `tts/`

